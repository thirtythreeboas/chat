## Available Scripts

In the project directory, you can run:
### `npm install`
### `npm start`

## Hints

Run the app. Enter your data from your acc. Then you need to enter a phone number. It appears on the left side of the window. Click on it and then you see input field to type text appears. That's it!